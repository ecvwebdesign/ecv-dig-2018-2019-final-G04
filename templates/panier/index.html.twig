{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}

    {% for autreProduit in userProduits %}
        <article style="display:flex; flex-wrap:wrap;">
            <h3>{{ autreProduit|first.libelle }}</h3>
            <img style="width:50px;height:50px;" src="{{ autreProduit|first.image }}" alt="">
            <p>{{ autreProduit|first.prix }} €</p>
            <p>{{ autreProduit|first.quantite }} restant</p>
            <p>{{ autreProduit|first.taille }}</p>
            <form action="{{  path("singleProduit", {"id": autreProduit|first.id}) }}">
                <input type="submit" value="Voir plus">
            </form>
        </article>
    {% endfor %}
{% endblock %}
